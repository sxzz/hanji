# Only the 2,064 characters common to all four places are prerendered; the rest
# are served by the app itself, which also resolves the regional forms
# (/char/国 -> /char/國) and raises its own 404. Static files win over this
# rule, so every prerendered page is still served as a file.
/char/*  /200  200
